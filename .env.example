# Set the verified publication origin in the build environment.
# It supplies canonical, metadataBase, social image URLs and sitemap entries.
# Without it the local preview emits no invented publication address.
# NEXT_PUBLIC_SITE_URL=

# Enable polling only if the filesystem does not deliver watch events.
# WATCH_POLLING=1
